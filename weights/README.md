# 权重下载

完整权重（MLX 格式，614MB，含 mmBERT encoder + 中文微调决策头）托管在 Hugging Face：

> **https://huggingface.co/[待发布后填入]** 

下载后放到本目录（`weights/ckpt-zh-v4-mlx/`），或任意路径后用 `laya_mlx.load(path)` 加载。

权重基于 convaiinnovations/laya-multilingual（Apache-2.0）微调，可自由使用/修改/分发。
